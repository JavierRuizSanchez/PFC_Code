<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Otros atributos de estilo -->

        <!-- Establece el tema de superposición para los menús emergentes -->
        <item name="popupTheme">@style/PopupOverlay</item>
    </style>

    <style name="PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AvisoText" parent="android:Widget.TextView">
        <item name="android:textColor">@color/black</item> <!-- Color rojo para indicar aviso -->
        <item name="android:textStyle">bold</item> <!-- Texto en negrita -->
        <!-- Otros atributos según tus preferencias -->
    </style>

    <style name="MyToggleButtonStyle" parent="android:Widget.Button.Toggle">
        <!-- Personaliza el estilo según tus preferencias -->
        <item name="android:textColor">#0000FF</item>
        <item name="android:background">@color/white</item>
        <!-- Otros atributos -->
    </style>
</resources>